// Ported from draco.js src/compression/attributes/prediction_schemes/PredictionSchemeDecoderInterface.js (MIT)
//
// The interface layer lives in PredictionSchemeDecoder (every scheme extends
// it); this module only keeps the name the sequential decoders type against.

export type { PredictionSchemeDecoder as PredictionSchemeDecoderInterface } from './PredictionSchemeDecoder'
